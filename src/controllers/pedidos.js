const crearPedido = (req,res)=>{
    res.send('Hola Mundo')
}

const verPedidos = (req,res) =>{
    res.send('Hola Mundo')
}

const verPedido = () =>{}

const modificarPedido = () =>{}

const eliminarPedido = () =>{}

module.exports = {
    crearPedido,
    verPedidos,
    verPedido,
    modificarPedido,
    eliminarPedido
}
